# All accounts are first looked up on chain and only the ones that are
# not found are either created as part of the transaction or result in
# account missing as usual
[accounts]
clone = { readonly = "all", writable = "all" }
create = true

# Additionally we turn off signature verification to allow
# unlimited experimentation
[validator]
sigverify = false
reset_ledger = false
