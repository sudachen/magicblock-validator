[package]
name = "triggercommit-program"
version.workspace = true
edition.workspace = true

[dependencies]
solana-program = { workspace = true }

[lib]
crate-type = ["cdylib", "lib"]

[features]
no-entrypoint = []
cpi = ["no-entrypoint"]
default = []
